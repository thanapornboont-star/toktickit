# Lab 3 — AI Use and Reflection

แบบบันทึกการใช้งาน AI / Agent ในการพัฒนาโปรเจกต์ **TokTickIT** (Lab 3: Users, Roles, IT Staff Ticketing, and Admin Screens)

---

## AI Model & Agent Environment
- **LLM / Model Name**: Gemini 3.8 Flash
- **Agent Framework**: Antigravity AI Coding Assistant (Google DeepMind)
- **Role**: Specification & Incremental Coding Agent (Spec DD, Test DD, and TDD)

---

## Selected Key Prompts (6–10 Prompts)

| # | Purpose | Selected Key Prompt (Shortened) | Agent Action & Output | What I Reviewed / Changed |
|:---:|---|---|---|---|
| 1 | Engineering Contract & Test Blueprint | "ช่วยทำ lab 3 ให้หน่อย lab 2 เสร็จแล้วทำต่อได้เลย... Issue 1 คือ Sprint 3 contract, acceptance criteria, and test blueprint #40" | วิเคราะห์ข้อกำหนด Lab Sheet 3 และ AI Agent Playbook สร้างสเปกครบ 6 ไฟล์: `specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`, `reviewer.md`, `ai-use.md` กำหนด 18 FRs, 26 BRs, 24 ACs, Data Models, Role Matrix, Status Transitions และ Planned Test Matrix | ตรวจสอบความถูกต้องของขอบเขต Lab 3 (ไม่เกินขอบเขต เช่น ไม่มี self-signup หรือ email delivery), ตรวจสอบความสอดคล้องของ Business Rules (BR-01 ถึง BR-26) และการป้องกัน Security boundaries |
| 2 | Identity Migration & Seed | "ทำต่อเลย (Work Item 2: Convert development requesters into Lab 3 users and seed roles)" | อัปเดต Prisma Schema เพิ่ม User, Role, Session, ITPriority, TicketStatus, PublicComment, InternalNote และฟิลด์บน Ticket; สร้าง Migration SQL ที่โอนย้ายข้อมูล DevRequester เดิม 251 tickets เข้า User โดยไม่มีข้อมูลสูญหาย; ปรับปรุง seed.ts ให้ครอบคลุม 11 users, 3 roles, sample tickets ทุกสถานะ และทดสอบ idempotency ผ่าน 100% | ตรวจสอบว่า existing tickets (251 ใบ) และ attachments (192 ไฟล์) ไม่สูญหาย, sequence ของ User ถูก sync อย่างถูกต้อง, และ automated tests เดิมของ Lab 1-2 ยังคงผ่านครบถ้วน (23/23 server, 21/21 client) |
| 3 | Authentication & First Password Change | "ต่อเลย (Work Item 3: Add login, current user, first-login password change, and logout)" | สร้าง Backend Auth API (`/login`, `/logout`, `/me`, `/change-password`), Password complexity validation (BR-04), Session management & token revocation (BR-06); สร้าง Frontend `Login` และ `ChangePassword` components พร้อม Busy state, Inline validation, Checklist, และ Authenticated App Shell showing user name, role badge, and logout | ตรวจสอบข้อความ Error เพื่อความปลอดภัย (Safe Failure) ไม่เปิดเผยว่าอีเมลมีอยู่ในระบบหรือไม่, ตรวจสอบว่าผู้ใช้ที่มี `mustChangePassword === true` ไม่สามารถเข้าใช้งานหน้าหลักได้ก่อนเปลี่ยนรหัสผ่าน, และรันเทสต์ผ่านครบถ้วน (Server 36/36, Client 35/35) |
| 4 | RBAC & Requester Continuity | "ต่อเลย (Work Item 4: Authorization boundary, RBAC enforcement, public comments, indicate-resolved)" | เพิ่ม `authenticateSessionOrDev` middleware รองรับ Bearer token (primary) และ `X-Dev-Requester-Id` header (fallback สำหรับ Lab 2 backward compat); บังคับ RBAC บน `/api/tickets/*` (REQUESTER only), `/api/staff/*` (IT_STAFF+ADMIN), `/api/admin/*` (ADMIN only); เพิ่ม `POST /api/tickets/:id/indicate-resolved` (AC-09, BR-16) และ `GET|POST /api/tickets/:id/public-comments` (AC-08, BR-17); ฝั่ง Client: เพิ่ม Public Comments thread และ Indicate Resolved button/banner ใน TicketDetail, resolved badge ใน MyTickets, `effectiveRequester` bridge ใน App.tsx; เขียน `authorization.api.test.ts` (17 tests pass) และ `RequesterContinuity.test.tsx` (4 tests pass) รวม server 53/53 + client 39/39 | ตรวจสอบว่า Lab 1-2 test suite ยังคงผ่านทั้งหมด (backward compat), ตรวจสอบว่า requesterId ที่ client ส่งมาถูก ignore และใช้ authenticated user แทน (BR-07, BR-10), ตรวจสอบว่า cross-user access คืน 404 ไม่ใช่ 403 (info disclosure prevention, BR-09) |
| 5 | IT Staff Work Queue | "Merge #43 แล้ว ทำ #44 ต่อได้เลย (Work Item 5: Build role-protected IT Staff work queue)" | พัฒนา Backend Staff API: `GET /api/staff/tickets` (queue พร้อม search, category/status/priority/owner filters, sorting, pagination), `GET /api/staff/tickets/:id` (full detail พร้อม requester, owner, category, system, active attachments), `GET /api/staff/members` (active staff dropdown); สร้าง Router guard ใน `app.ts` ป้องกัน REQUESTER ด้วย 403; สร้าง Frontend Component `StaffTicketQueue.tsx` รองรับ toolbar filters, table (desktop), cards (mobile), pagination, priority/status badges; เขียน `staff-queue.api.test.ts` (18 tests pass) และ `StaffTicketQueue.test.tsx` (11 tests pass) รวม server 71/71 + client 50/50 | ตรวจสอบว่า IT Staff และ Admin เท่านั้นที่เข้าถึงคิวงานได้ (AC-11, BR-10, BR-19), ป้องกัน 403 เมื่อ Requester พยายามเรียกคิวงาน, และรองรับ responsive layout สลับระหว่าง table กับ card บนอุปกรณ์หน้าจอเล็ก |
| 6 | Staff Operations, Comments & Notes | "ต่อเลย (Work Item 6: Implement Staff Ticket Detail workflow and communications)" | พัฒนา Backend Staff Operations: `PATCH /api/staff/tickets/:id/owner` (Claim, Reassign, Unassign), `PATCH /api/staff/tickets/:id/priority` (LOW, MEDIUM, HIGH), `PATCH /api/staff/tickets/:id/status` (State machine บังคับตาม BR-15 และดัก transition ต้องห้ามด้วย 400 Bad Request), `GET/POST /api/staff/tickets/:id/internal-notes` (เก็บบันทึกภายใน ป้องกัน Requester ด้วย 403 Forbidden); พัฒนา Frontend Component `StaffTicketDetail.tsx` รองรับการ Claim/Assign งาน, ปรับ IT Priority, เปลี่ยนสถานะตาม BR-15, แสดง Public Comments และ Internal Notes ที่มีสไตล์สีเหลืองเตือนความลับต่างจาก Public Comment ชัดเจน; เขียน `staff-ticket-detail.api.test.ts` (17 tests), `comments-notes.api.test.ts` (16 tests), `StaffTicketDetail.test.tsx` (8 tests) รวม server 104/104 + client 58/58 | ตรวจสอบ State Transition Matrix (BR-15) ว่าไม่สามารถกระโดดข้ามสถานะได้ เช่น NEW ไป CLOSED ต้องถูกปฏิเสธด้วย 400, ตรวจสอบว่า Internal Notes ถูกบล็อกไม่ให้ Requester เข้าถึงได้ 100% (BR-18, AC-10), และตรวจสอบ UI ว่าข้อความ Public กับ Internal มี visual distinction ชัดเจน |
| 7 | Administrator User Management | "ต่อเลย (Work Item 7: Build Administrator User Management views and maintenance flows)" | พัฒนา Backend Admin API ใน `server/src/routes/admin.ts`: `GET /api/admin/users` (search ชื่อ/อีเมล, filter ตาม role), `POST /api/admin/users` (สร้างผู้ใช้ใหม่พร้อม 1 role และ initial password บังคับ complexity, duplicate email 409 conflict, ตั้ง mustChangePassword: true), `PATCH /api/admin/users/:id` (อัปเดตข้อมูลผู้ใช้, ป้องกัน self-deactivation ด้วย 400 ตาม BR-21, ป้องกัน deactivation หรือ demote ผู้ดูแลระบบคนสุดท้ายด้วย 400 ตาม BR-22), `POST /api/admin/users/:id/reset-password` (กำหนดรหัสผ่านชั่วคราวใหม่ บังคับเปลี่ยนรหัสผ่านในการล็อกอินครั้งถัดไปและยกเลิก active sessions ทั้งหมด); พัฒนา Frontend Component `UserManagement.tsx` พร้อม Table รายชื่อผู้ใช้, Role/Status Badges, Create User modal, Edit User modal พร้อม deactivation guard และ Reset Password form; เขียนเทสต์ `users-admin.api.test.ts` (20 tests pass) และ `UserManagement.test.tsx` (6 tests pass) รวม server 124/124 + client 64/64 | ตรวจสอบการป้องกันความปลอดภัย: ห้าม Administrator ปิดการใช้งานตนเอง (BR-21, AC-20), ป้องกันไม่ให้ระบบเหลือ Administrator ที่ active น้อยกว่า 1 คน (BR-22, AC-21), ป้องกันการสร้างอีเมลซ้ำ (BR-20, AC-19), ป้องกันไม่ให้สิทธิ์อื่นเข้าถึง Admin endpoint (BR-10, AC-23 ได้รับ 403 Forbidden), และทดสอบ TypeScript build ผ่าน 0 error |
| 8 | Shell Navigation & Responsive Visual QA | "merge แล้วต่อไป Finish role navigation, responsive behavior, and Zen Green visual QA (WI-8, closes #47)" | ตรวจสอบและยืนยัน Navigation Architecture ใน `App.tsx`: Requester แสดง My Tickets + Create Ticket, IT Staff แสดงเฉพาะ Ticket Queue, Administrator แสดงเฉพาะ User Management — ไม่มีการ cross-role link รั่ว; เพิ่ม CSS Tablet breakpoint `(768–991px)` ใน `App.css` พร้อม `.queue-filter-toolbar` flex-wrap rule, `.user-table-responsive` overflow scroll; ปรับปรุง Mobile `(<768px)` rules: `.main-nav a` มี `min-height: 44px` touch target (AC-24, BR-26), `.btn` มี `min-height: 44px`, modal drawer `border-radius: 16px 16px 0 0`, `.queue-card` background; เพิ่ม CSS class `queue-filter-toolbar` ใน `StaffTicketQueue.tsx` และ `user-table-responsive` ใน `UserManagement.tsx`; สร้าง `client/tests/lab-03/AppShell.test.tsx` (UI-06, 10 tests) ตรวจสอบ: Requester-only nav links, IT Staff-only nav links, Admin-only nav links, Sign Out callback, in-shell navigation, app-header class, brand link; อัปเดต `tests.md` เพิ่ม UI-06 row และทำเครื่องหมาย Responsive Viewport Checklist ครบทุก 3 ข้อ | ตรวจสอบว่า nav link ของแต่ละ role ไม่รั่วข้ามกัน เช่น Requester ไม่เห็น Ticket Queue, Staff ไม่เห็น User Management; ตรวจสอบว่า mobile CSS ไม่ break layout ที่มีอยู่เดิม; รันเทสต์ทั้งหมดผ่านครบถ้วน (Server 124/124, Client 74/74) |
| 9 | E2E & Full Traceability Closure | "Merge เรียบร้อยแล้วทำ Complete Sprint 3 E2E flows and final test traceability #48 ต่อได้เลย" | พัฒนาชุดทดสอบ Playwright E2E ครบ 3 ชุด: `authentication.spec.ts` (E2E-01: AC-01..05 ทดสอบ Login, Safe invalid credential error, Deactivated account rejection, Mandatory first password change checklist และ Logout invalidation/back-navigation protection), `staff-ticket-flow.spec.ts` (E2E-02: AC-11..16 ทดสอบ Queue search & filters, Claim ticket, IT priority, Permitted status transitions, Dual communication threads - Public comment & Amber confidential internal note, Requester indicated resolved indicator), `user-administration.spec.ts` (E2E-03: AC-17..23 ทดสอบ User directory, Add user พร้อม 1 role, Duplicate email rejection, Admin self-deactivation guard, Reset initial password, Non-admin 403 access control); สร้าง helper `helpers.ts` พร้อม automated database re-seed และ screenshot capture ลง `artifacts/lab-03/screenshots/` (auth, staff, admin) ครบ 3 viewports (Desktop, Tablet, Mobile) รวม 18/18 tests pass (100%); อัปเดต `tests.md` E2E status เป็น Pass ครบทั้งหมด | ตรวจสอบและปรับปรุง locators ให้รองรับ responsive DOM (ใช้ `:visible` เพื่อป้องกัน strict mode violation และหลีกเลี่ยงการจับ hidden table บน mobile card view), ตรวจสอบว่า accessible names ของ button ตรงกับ `aria-label` จริงใน component (`Update status`, `Back to ticket queue`, `Add new user`), ตรวจสอบการ invalidation session เมื่อ logout และ back navigation ไม่สามารถย้อนกลับเข้า protected shell ได้ |
| 10 | Release Integration & Evidence Pack | "merge เรียบร้อยแล้ว ต่อไป issue สุดท้าย Finalize Lab 3 evidence and release integration #49 และก็อย่าลืมเปลี่ยน reviewer ให้ด้วย ของเรา pull/58 ของเพื่อน pull/52 และ 53" | อัปเดต `docs/lab-03/reviewer.md` เพิ่ม PR #58 (ของเรา WI-9) ในตาราง authored PRs พร้อม detailed review section จาก @jiraphat-j, เพิ่ม PR #52 และ #53 (ของเพื่อน WI-9 และ WI-10) ในตาราง reviewed PRs พร้อม detailed review sections ที่เราทำให้เพื่อน; อัปเดต Row 10 ใน `ai-use.md`; เตรียม PR สำหรับ `sprint3/finalize-lab3-evidence` → `lab3-staging` เป็น WI-10 สุดท้ายก่อน release | ตรวจสอบว่า reviewer.md ครบถ้วนทั้งตาราง Summary และ Detailed sections ของทุก PR ทั้งที่เราทำและที่เราตรวจ ไม่มีรายการใดขาดหาย |

---

## Reflection (บทสะท้อนความคิด)

*[ส่วนนี้สำหรับนักศึกษาเขียนสะท้อนความคิดด้วยตนเอง หลังจากการพัฒนาและทดสอบครบถ้วนตามเกณฑ์ของวิชา]*
