{
  "name": "toktickit-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace for Lab 2 Playwright E2E and visual QA tests. Client and server each have their own package.json.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
